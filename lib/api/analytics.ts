// Analytics endpoints removed
export const analyticsApi = {
  getAnonymizedAnalytics: async () => {
    // Stub function to prevent errors - analytics endpoints were removed
    throw new Error('Analytics endpoints have been removed. This feature is no longer available.');
  },
};

// Types removed
export interface AnonymizedAnalytics {}
